# Runtime deps without PyTorch — torch is installed separately (see below).
-r requirements-base.txt

# PyTorch is NOT listed here on purpose: default PyPI torch wheels bundle CUDA (~GB).
#
# Docker (CPU):  libs/docker-torch/install_cpu.sh in Dockerfile
# Local CPU dev: pip install "torch==2.2.0" --index-url https://download.pytorch.org/whl/cpu
#                then pip install -r requirements-base.txt
