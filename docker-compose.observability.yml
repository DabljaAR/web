# Observability overlay — Loki, Grafana, VictoriaMetrics, Tempo, exporters.
# Opt-in via COMPOSE_PROFILES=observability in .env.production
#
#   docker compose --env-file .env.production \
#     -f docker-compose.microservices.prod.yml \
#     -f docker-compose.observability.yml up -d

services:
  loki:
    profiles: [observability]
    image: grafana/loki:3.4.2
    container_name: dabljaar_loki
    command: -config.file=/etc/loki/config.yml
    volumes:
      - ./infra/observability/loki-config.yml:/etc/loki/config.yml:ro
      - loki_data:/loki
    mem_limit: 512m
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3100/ready"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  promtail:
    profiles: [observability]
    image: grafana/promtail:3.4.2
    container_name: dabljaar_promtail
    command: -config.file=/etc/promtail/config.yml
    volumes:
      - ./infra/observability/promtail-config.yml:/etc/promtail/config.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
    mem_limit: 128m
    depends_on:
      - loki
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  victoriametrics:
    profiles: [observability]
    image: victoriametrics/victoria-metrics:v1.109.1
    container_name: dabljaar_victoriametrics
    command:
      - "--storageDataPath=/storage"
      - "--retentionPeriod=15d"
      - "--promscrape.config=/etc/vm/scrape.yml"
      - "--promscrape.config.strictParse=false"
      - "--httpListenAddr=:8428"
    volumes:
      - ./infra/observability/victoriametrics/scrape.yml:/etc/vm/scrape.yml:ro
      - vm_data:/storage
    mem_limit: 512m
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  tempo-init:
    profiles: [observability]
    image: grafana/tempo:2.7.1
    container_name: dabljaar_tempo_init
    user: "0:0"
    entrypoint: ["/bin/sh", "-c", "chown -R 10001:10001 /var/tempo"]
    volumes:
      - tempo_data:/var/tempo
    restart: "no"
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  tempo:
    profiles: [observability]
    image: grafana/tempo:2.7.1
    container_name: dabljaar_tempo
    command: ["-config.file=/etc/tempo/config.yml"]
    volumes:
      - ./infra/observability/tempo-config.yml:/etc/tempo/config.yml:ro
      - tempo_data:/var/tempo
    mem_limit: 384m
    depends_on:
      tempo-init:
        condition: service_completed_successfully
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  otel-collector:
    profiles: [observability]
    image: otel/opentelemetry-collector-contrib:0.120.0
    container_name: dabljaar_otel_collector
    command: ["--config=/etc/otel-collector.yml"]
    volumes:
      - ./infra/observability/otel-collector.yml:/etc/otel-collector.yml:ro
    mem_limit: 128m
    depends_on:
      tempo:
        condition: service_started
      victoriametrics:
        condition: service_started
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  grafana:
    profiles: [observability]
    image: grafana/grafana:11.5.2
    container_name: dabljaar_grafana
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_SERVER_ROOT_URL: https://grafana.${DOMAIN}
      GF_AUTH_ANONYMOUS_ENABLED: "false"
      GF_UNIFIED_ALERTING_ENABLED: "true"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./infra/observability/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./infra/observability/grafana/dashboards:/var/lib/grafana/dashboards:ro
    mem_limit: 256m
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 60s
    depends_on:
      - loki
      - victoriametrics
      - tempo
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  node_exporter:
    profiles: [observability]
    image: prom/node-exporter:v1.9.0
    container_name: dabljaar_node_exporter
    command:
      - "--path.procfs=/host/proc"
      - "--path.sysfs=/host/sys"
      - "--path.rootfs=/rootfs"
      - "--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)"
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    mem_limit: 64m
    pid: host
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

  cadvisor:
    profiles: [observability]
    image: gcr.io/cadvisor/cadvisor:v0.51.0
    container_name: dabljaar_cadvisor
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
    mem_limit: 128m
    privileged: true
    restart: unless-stopped
    networks:
      - dabljaar_network

  postgres_exporter:
    profiles: [observability]
    image: prometheuscommunity/postgres-exporter:v0.16.0
    container_name: dabljaar_postgres_exporter
    environment:
      DATA_SOURCE_NAME: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-dabljaar}?sslmode=disable
    mem_limit: 64m
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    networks:
      - dabljaar_network

volumes:
  loki_data:
  grafana_data:
  vm_data:
  tempo_data:
