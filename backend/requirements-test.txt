# Test Dependencies for DabljaAR Backend
# Install with: pip install -r requirements-test.txt

# Core testing framework
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Testing utilities
httpx>=0.24.0  # For async HTTP client testing
faker>=19.0.0  # For generating test data

# Mocking and fixtures
responses>=0.23.0  # Mock HTTP responses
freezegun>=1.2.0  # Mock datetime

# Coverage reporting
coverage>=7.2.0
coverage[toml]>=7.2.0

# Code quality (optional but recommended)
ruff>=0.14.0  # Linter + formatter + import sorting rules
mypy>=1.4.0   # Type checker

# Documentation testing (optional)
pytest-html>=3.2.0  # HTML test reports